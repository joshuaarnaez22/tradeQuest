import { PhasePlaceholder } from "@/components/PhasePlaceholder";

// TODO(Phase 4): resolve today's puzzle via lib/puzzle-of-day.ts, render ReplayChart + DecisionControls.
export default function ReplayPage() {
  return <PhasePlaceholder title="Replay" note="Today's puzzle lands here in Phase 4." />;
}
