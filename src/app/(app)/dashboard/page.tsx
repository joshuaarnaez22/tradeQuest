import { PhasePlaceholder } from "@/components/PhasePlaceholder";

// TODO(Phase 7): render StreakFlame + XPBar + recent-sessions list from derived attempts data.
export default function DashboardPage() {
  return <PhasePlaceholder title="Dashboard" note="Streak, XP, and recent sessions land here in Phase 7." />;
}
